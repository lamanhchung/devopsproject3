- name: "Upgrade apt"
  become: true
  apt:
    upgrade: yes
    update_cache: yes

- name: "Install dependency"
  become: true
  apt:
    name: ["tar", "nodejs", "npm"]
    state: "latest"
    update_cache: yes